package sec06.ch03;

public class LogicalOperatorExam {
	public static void main(String[] args) {
		// &&(and), ||(or), !(not)
		
		// && 논리 연산자는 앞의 것이 false이면 뒤의 것은 실행하지 않는다.
		// || 논리 연산자는 앞의 것이 true면 뒤의 것은 실행하지 않는다.
	}
}
